export const ejemploMap = `// Ejemplos de Map en JS
const mp = new Map();
mp.set('color','azul');
mp.set(1, true);
const obj = {id: 1};
mp.set(obj, 'valor');
console.log(mp.get('color'));
console.log(mp.get(obj));`;
